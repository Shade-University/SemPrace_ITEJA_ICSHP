var a, b, c, angle, i;

a = 10;
b = 10;
c = 20;

i = 0;
angle = 60;

if a + b <= c then { func write("error") }
if a + c <= b then { func write("error") }
if b + c <= a then { func write("error") }



for i to 200 do
{
	func pen(down)
	func angle(90)
	
	func forward(c)
	func angle(angle)
	func forward(a)
	func angle(angle)
	func forward(b)
	
	i = i + 50;
	func pen(up)
	func angle(90)
	func forward(i)
}


END.